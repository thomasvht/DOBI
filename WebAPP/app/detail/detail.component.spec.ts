/**
 * Created by Sander Verkaemer on 08/01/2017.
 */
import { DetailComponent } from './detail.component';

// WERKT NIET DOOR SOCKETS

/*
describe('DetailComponent', ()=>{
  let comp = new DetailComponent();

  it("Should exist", () =>{
    expect(comp).toBeTruthy();
  });

  it("Should have property 'id'", ()=>{
    expect(comp.id).toBeDefined();
  });

  it("Should have property 'id' of type string", () =>{
    expect(typeof comp.id).toEqual('string');
  });

  it("Should have property 'number'", ()=>{
    expect(comp.number).toBeDefined();
  });

  it("Should have property 'number' of type number", () =>{
    expect(typeof comp.number).toEqual('number');
  });

  it("Should have property 'user'", ()=>{
    expect(comp.user).toBeDefined();
  });

  it("Should have property 'user' of type string", () =>{
    expect(typeof comp.user).toEqual('string');
  });

  it("Should have property 'email'", ()=>{
    expect(comp.email).toBeDefined();
  });

  it("Should have property 'email' of type string", () =>{
    expect(typeof comp.email).toEqual('string');
  });
});

*/
